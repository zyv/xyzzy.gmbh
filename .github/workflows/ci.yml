name: CI

on: [ push ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v3

      - run: npm ci

      - run: npm run lint

      - run: npm run prettier-check
